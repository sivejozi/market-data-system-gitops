apiVersion: v1
kind: Service
metadata:
  name: validation-prediction-service
  namespace: marketguard
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  selector:
    app: validation-prediction-service
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP