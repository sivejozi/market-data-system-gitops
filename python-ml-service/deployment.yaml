apiVersion: apps/v1
kind: Deployment
metadata:
  name: python-ml-service
  namespace: marketguard
  annotations:
    argocd.argoproj.io/sync-wave: "1"    # ← deploy second
spec:
  replicas: 1
  selector:
    matchLabels:
      app: python-ml-service
  template:
    metadata:
      labels:
        app: python-ml-service
    spec:
      containers:
        - name: python-ml-service
          image: ghcr.io/sivejozi/python-ml-service:latest
          ports:
            - containerPort: 8000
          env:
            - name: ENVIRONMENT
              valueFrom:
                configMapKeyRef:
                  name: python-ml-config
                  key: ENVIRONMENT
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "500m"
          readinessProbe:
            httpGet:
              path: /
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: 8000
            initialDelaySeconds: 60
            periodSeconds: 20